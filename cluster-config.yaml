apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: my-eks-cluster
  region: us-east-1

managedNodeGroups:
  - name: standard-workers
    instanceType: c3.large
    minSize: 2
    maxSize: 4