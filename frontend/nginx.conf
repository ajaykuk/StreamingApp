server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # This is the magic line that fixes SPA routing
        try_files $uri $uri/ /index.html;
    }
}